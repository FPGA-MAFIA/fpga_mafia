#include "../../../app/defines/big_core_defines.h"

int main() {
    float a = 1.5, b = 2.5;
    
    // printf("%.2f + %.2f = %.2f\n", a, b, a+b);
    // printf("%.2f - %.2f = %.2f\n", a, b, a-b);

    return 0;
}

